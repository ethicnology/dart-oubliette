group = "com.oubliette.keystore"
version = "1.0.0"

buildscript {
    ext.kotlin_version = "2.4.0"
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath("com.android.tools.build:gradle:9.2.0")
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version")
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

apply plugin: "com.android.library"

android {
    namespace = "com.oubliette.keystore"

    compileSdk = 36

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    sourceSets {
        main.java.srcDirs += "src/main/kotlin"
        test.java.srcDirs += "src/test/kotlin"
    }

    defaultConfig {
        // API 30 (Android 11) floor: required so setUserAuthenticationParameters
        // is always available. On API 29 the authenticated profiles would
        // silently generate a key with no user-auth requirement.
        minSdk = 30
    }

    dependencies {
        // kotlin-test-junit5 binds the kotlin.test `@Test` annotations to the
        // JUnit 5 Jupiter engine. Plain `kotlin-test` defaults to JUnit4, which
        // the `useJUnitPlatform()` runner below cannot discover without an
        // engine on the classpath — the contract test would silently not run.
        testImplementation("org.jetbrains.kotlin:kotlin-test-junit5")
    }

    testOptions {
        unitTests.all {
            useJUnitPlatform()

            testLogging {
               events "passed", "skipped", "failed", "standardOut", "standardError"
               outputs.upToDateWhen {false}
               showStandardStreams = true
            }
        }
    }
}

// AGP 9 compiles Kotlin built-in (no kotlin-android plugin). Configure the JVM
// target via the kotlin extension's compilerOptions (kotlinOptions is removed).
kotlin {
    compilerOptions {
        jvmTarget = org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_17
    }
}
